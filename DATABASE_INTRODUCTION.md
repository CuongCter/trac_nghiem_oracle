# Giới thiệu Cơ sở dữ liệu — Hệ thống Quản lý Thi Trắc Nghiệm

> **Dự án:** Hệ thống Quản lý Thi Trắc Nghiệm (Online Multiple-Choice Exam Management System)
> **Hệ quản trị CSDL:** **Oracle Database 19c**
> **Mục đích file:** Tổng quan, giới thiệu về CSDL đang được sử dụng trong dự án — phục vụ cho việc onboarding thành viên mới, thuyết trình học thuật và tài liệu kỹ thuật.

---

## Mục lục

1. [Tổng quan dự án](#1-tổng-quan-dự-án)
2. [Vì sao chọn Oracle 19c?](#2-vì-sao-chọn-oracle-19c)
3. [Kiến trúc hệ thống & vai trò của CSDL](#3-kiến-trúc-hệ-thống--vai-trò-của-csdl)
4. [Mô hình dữ liệu (Data Model)](#4-mô-hình-dữ-liệu-data-model)
5. [Danh sách bảng chi tiết](#5-danh-sách-bảng-chi-tiết)
6. [Ràng buộc toàn vẹn (Constraints)](#6-ràng-buộc-toàn-vẹn-constraints)
7. [Chỉ mục (Indexes)](#7-chỉ-mục-indexes)
8. [Kết nối từ Backend](#8-kết-nối-từ-backend)
9. [Cài đặt & khởi tạo](#9-cài-đặt--khởi-tạo)
10. [Các tính năng nổi bật của CSDL trong dự án](#10-các-tính-năng-nổi-bật-của-csdl-trong-dự-án)
11. [Cấu trúc file liên quan](#11-cấu-trúc-file-liên-quan)

---

## 1. Tổng quan dự án

Hệ thống Quản lý Thi Trắc Nghiệm là một nền tảng web **3 vai trò** phục vụ cho việc tổ chức các kỳ thi trắc nghiệm trực tuyến:

| Vai trò | Quyền hạn chính |
|---|---|
| **ADMIN** | Quản lý người dùng, môn học, lớp học, đề thi, xem báo cáo tổng quan. |
| **TEACHER** | Tạo ngân hàng câu hỏi, soạn đề thi (thủ công hoặc random theo độ khó), theo dõi gian lận realtime, xem kết quả. |
| **STUDENT** | Làm bài thi được phân công theo lớp, xem điểm và lịch sử. |

**Quy trình cốt lõi:**

```
Tạo ngân hàng câu hỏi → Soạn đề → Giao cho lớp → Học viên làm bài → Chấm điểm tự động → Báo cáo
```

**Stack công nghệ:**

| Tầng | Công nghệ |
|---|---|
| Frontend | Next.js 14 · React 18 · TypeScript · Ant Design 5 |
| Backend | Node.js 20 · Express 4 · TypeScript |
| **CSDL** | **Oracle Database 19c** (driver `oracledb` 6.x với connection pool) |
| Realtime | Socket.IO (cảnh báo gian lận tới giáo viên) |
| Auth | JWT (stateless) + bcrypt |

---

## 2. Vì sao chọn Oracle 19c?

Oracle 19c được lựa chọn làm hệ quản trị CSDL cho dự án vì những lý do sau:

### 2.1. Phù hợp với bối cảnh đào tạo

Dự án được thực hiện trong khuôn khổ môn học **"Quản trị CSDL nâng cao"** (Thạc sĩ). Oracle 19c là hệ quản trị CSDL quan hệ doanh nghiệp phổ biến nhất, là nền tảng lý tưởng để thực hành các kỹ thuật nâng cao.

### 2.2. Toàn vẹn dữ liệu mạnh mẽ

Oracle hỗ trợ đầy đủ các cơ chế đảm bảo toàn vẹn dữ liệu ở **tầng database** (không phụ thuộc vào ứng dụng):

- **Primary Key / Foreign Key** với các chiến lược `ON DELETE CASCADE` và `ON DELETE SET NULL`.
- **CHECK constraint** cho phép ràng buộc giá trị (ví dụ: `STATUS IN ('ACTIVE','LOCKED')`, `DIFFICULTY IN ('EASY','MEDIUM','HARD')`).
- **UNIQUE constraint** cho các trường định danh duy nhất (email, mã môn học, tên lớp...).
- **TIMESTAMP** với `DEFAULT SYSTIMESTAMP` cho phép tự động ghi nhận thời điểm tạo/cập nhật.

### 2.3. Sequence tự tăng (IDENTITY)

Sử dụng `GENERATED BY DEFAULT ON NULL AS IDENTITY` cho mọi khóa chính — cơ chế chuẩn Oracle 12c+ giúp sinh khóa tự động, an toàn và concurrent-friendly.

### 2.4. Hỗ trợ Unicode đầy đủ

Toàn bộ cột văn bản dùng kiểu `VARCHAR2(n CHAR)` — đảm bảo tiếng Việt có dấu được lưu trữ và so sánh chính xác.

### 2.5. Khả năng mở rộng cao

- **Index** trên các cột truy vấn thường xuyên.
- **Connection Pool** từ driver `oracledb` giúp tối ưu hiệu năng cho hệ thống đa người dùng.
- Sẵn sàng cho các kỹ thuật nâng cao: Trigger, Procedure, Function, Partitioning, Materialized View...

---

## 3. Kiến trúc hệ thống & vai trò của CSDL

```
┌──────────────────┐    HTTP / WebSocket    ┌──────────────────┐
│   Frontend       │ ◄────────────────────► │   Backend        │
│   Next.js 14     │   (JWT, REST, Socket)  │   Node + Express │
│   (FE người dùng)│                        │   (TS)           │
└──────────────────┘                        └────────┬─────────┘
                                                      │ oracledb pool
                                                      ▼
                                            ┌──────────────────┐
                                            │  Oracle 19c      │
                                            │  (11 bảng chính) │
                                            └──────────────────┘
```

**CSDL là trung tâm của hệ thống**, đảm nhận:

1. Lưu trữ toàn bộ dữ liệu nghiệp vụ (người dùng, môn học, câu hỏi, đề thi, lượt thi, kết quả, vi phạm).
2. Đảm bảo toàn vẹn dữ liệu thông qua các ràng buộc ở tầng DB.
3. Cung cấp khả năng truy vấn đồng thời từ nhiều request nhờ connection pool.
4. Là nguồn dữ liệu duy nhất (single source of truth) cho cả Admin, Teacher, Student.

---

## 4. Mô hình dữ liệu (Data Model)

### 4.1. Sơ đồ ER tổng quan

```
USERS ─┬─< SUBJECTS ─< QUESTIONS
       ├─< CLASSES ─< CLASS_STUDENTS >─ USERS (role=STUDENT)
       ├─< EXAMS ─< EXAM_QUESTIONS >─ QUESTIONS
       │        └─< EXAM_CLASSES >─ CLASSES
       └─< EXAM_ATTEMPTS ─< ATTEMPT_ANSWERS >─ QUESTIONS
                     └─< VIOLATIONS
       └─< EXAM_RESULTS ─< RESULT_ANSWERS >─ QUESTIONS
```

### 4.2. Các nhóm bảng nghiệp vụ

| Nhóm | Bảng | Mô tả |
|---|---|---|
| **Người dùng & phân quyền** | `USERS` | Tài khoản + vai trò (Admin / Teacher / Student) |
| **Danh mục** | `SUBJECTS`, `CLASSES`, `CLASS_STUDENTS` | Môn học, lớp, quan hệ N-N giữa lớp và học viên |
| **Ngân hàng câu hỏi** | `QUESTIONS` | Câu hỏi trắc nghiệm 4 đáp án |
| **Đề thi** | `EXAMS`, `EXAM_QUESTIONS`, `EXAM_CLASSES` | Đề + bảng trung gian gán câu hỏi & lớp |
| **Lượt thi** | `EXAM_ATTEMPTS`, `ATTEMPT_ANSWERS` | Quá trình học viên làm bài (chưa chấm) |
| **Gian lận** | `VIOLATIONS` | Nhật ký vi phạm (tab switch, fullscreen exit...) |
| **Kết quả** | `EXAM_RESULTS`, `RESULT_ANSWERS` | Điểm và đáp án sau khi chấm |

---

## 5. Danh sách bảng chi tiết

### 5.1. `USERS` — Người dùng hệ thống

| Cột | Kiểu | Ràng buộc | Mô tả |
|---|---|---|---|
| `ID` | `NUMBER IDENTITY` | PK | Khóa chính tự tăng |
| `FULL_NAME` | `VARCHAR2(120 CHAR)` | NOT NULL | Họ và tên |
| `EMAIL` | `VARCHAR2(180 CHAR)` | NOT NULL, UNIQUE | Email đăng nhập |
| `PASSWORD_HASH` | `VARCHAR2(255 CHAR)` | NOT NULL | Mật khẩu đã băm (bcrypt) |
| `ROLE` | `VARCHAR2(20 CHAR)` | CHECK IN ('ADMIN','TEACHER','STUDENT') | Vai trò |
| `STATUS` | `VARCHAR2(20 CHAR)` | CHECK IN ('ACTIVE','LOCKED'), DEFAULT 'ACTIVE' | Trạng thái tài khoản |
| `CREATED_AT` | `TIMESTAMP` | DEFAULT SYSTIMESTAMP | Ngày tạo |
| `UPDATED_AT` | `TIMESTAMP` | DEFAULT SYSTIMESTAMP | Ngày cập nhật |

### 5.2. `SUBJECTS` — Môn học

| Cột | Kiểu | Ràng buộc | Mô tả |
|---|---|---|---|
| `ID` | `NUMBER IDENTITY` | PK | Khóa chính |
| `NAME` | `VARCHAR2(120 CHAR)` | NOT NULL | Tên môn |
| `CODE` | `VARCHAR2(20 CHAR)` | NOT NULL, UNIQUE | Mã môn học |
| `DESCRIPTION` | `VARCHAR2(500 CHAR)` | — | Mô tả |
| `CREATED_BY` | `NUMBER` | FK → USERS(ID) ON DELETE SET NULL | Người tạo |

### 5.3. `QUESTIONS` — Ngân hàng câu hỏi

| Cột | Kiểu | Ràng buộc | Mô tả |
|---|---|---|---|
| `ID` | `NUMBER IDENTITY` | PK | Khóa chính |
| `SUBJECT_ID` | `NUMBER` | NOT NULL, FK → SUBJECTS ON DELETE CASCADE | Môn học |
| `CONTENT` | `VARCHAR2(2000 CHAR)` | NOT NULL | Nội dung câu hỏi |
| `OPTION_A/B/C/D` | `VARCHAR2(500 CHAR)` | NOT NULL | 4 đáp án |
| `CORRECT_ANSWER` | `CHAR(1)` | CHECK IN ('A','B','C','D') | Đáp án đúng |
| `DIFFICULTY` | `VARCHAR2(20 CHAR)` | CHECK IN ('EASY','MEDIUM','HARD') | Độ khó |
| `CHAPTER` | `VARCHAR2(80 CHAR)` | — | Chương |
| `POINT` | `NUMBER(6,2)` | CHECK > 0 AND <= 100, DEFAULT 1 | Điểm |
| `CREATED_BY` | `NUMBER` | FK → USERS ON DELETE SET NULL | Người tạo |

### 5.4. `CLASSES` & `CLASS_STUDENTS` — Lớp học

- **`CLASSES`** lưu thông tin lớp: tên (UNIQUE), giáo viên phụ trách (`TEACHER_ID` FK → USERS).
- **`CLASS_STUDENTS`** là bảng trung gian N-N giữa lớp và học viên (PK ghép `CLASS_ID + STUDENT_ID`).

### 5.5. `EXAMS` — Đề thi

| Cột | Ràng buộc đáng chú ý |
|---|---|
| `STATUS` | `CHECK IN ('DRAFT','PUBLISHED','CLOSED')` — vòng đời đề thi |
| `SHUFFLE_QUESTIONS` / `SHUFFLE_OPTIONS` | `CHAR(1) DEFAULT 'Y'` — xáo trộn câu / đáp án |
| `DURATION_MINUTES` | `CHECK > 0` |
| `END_TIME` | `CHECK > START_TIME` |
| `TOTAL_QUESTIONS`, `TOTAL_POINTS` | `DEFAULT 0` |

### 5.6. `EXAM_QUESTIONS` & `EXAM_CLASSES`

Hai bảng trung gian gán **câu hỏi** và **lớp** vào đề thi (quan hệ N-N).

### 5.7. `EXAM_ATTEMPTS` — Lượt thi

- `UNIQUE(EXAM_ID, STUDENT_ID)` — **mỗi học viên chỉ có 1 lượt thi cho mỗi đề**.
- `STATUS IN ('IN_PROGRESS','SUBMITTED','EXPIRED')`.
- `IS_AUTO_SUBMITTED CHAR(1) DEFAULT 'N'` — đánh dấu bài nộp tự động (do hết giờ hoặc vượt ngưỡng vi phạm).

### 5.8. `ATTEMPT_ANSWERS` — Đáp án đang chọn (trước khi chấm)

- `SELECTED_OPTION CHAR(1)` có thể `NULL` (chưa trả lời).
- PK ghép `(ATTEMPT_ID, QUESTION_ID)`.

### 5.9. `VIOLATIONS` — Nhật ký gian lận

- Ghi nhận mỗi lần học viên vi phạm (tab switch, fullscreen exit, copy/paste...).
- `OCCURRED_AT DEFAULT SYSTIMESTAMP` + index `(ATTEMPT_ID, OCCURRED_AT)` giúp truy vấn nhanh theo lượt thi.

### 5.10. `EXAM_RESULTS` & `RESULT_ANSWERS` — Kết quả sau chấm

- `TOTAL_CORRECT`, `TOTAL_WRONG`, `SCORE NUMBER(6,2)`, `PASSED CHAR(1)`.
- `UNIQUE(EXAM_ID, STUDENT_ID)` — đảm bảo 1 kết quả / 1 học viên / 1 đề.
- `RESULT_ANSWERS` lưu `IS_CORRECT CHAR(1)` cho từng câu.

---

## 6. Ràng buộc toàn vẹn (Constraints)

CSDL sử dụng nhiều lớp ràng buộc để đảm bảo dữ liệu luôn hợp lệ:

### 6.1. Khóa chính (PRIMARY KEY)

Mọi bảng đều có `PRIMARY KEY` — đa số dùng `NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY` để Oracle tự sinh giá trị.

### 6.2. Khóa ngoại (FOREIGN KEY) với chiến lược xóa

| Quan hệ | Hành vi khi xóa | Mục đích |
|---|---|---|
| `QUESTIONS.SUBJECT_ID` → `SUBJECTS` | `ON DELETE CASCADE` | Xóa môn → xóa luôn câu hỏi môn đó |
| `QUESTIONS.CREATED_BY` → `USERS` | `ON DELETE SET NULL` | Xóa user → giữ câu hỏi, set null người tạo |
| `EXAMS.SUBJECT_ID` → `SUBJECTS` | `ON DELETE CASCADE` | Xóa môn → xóa đề của môn |
| `EXAM_ATTEMPTS.STUDENT_ID` → `USERS` | `ON DELETE CASCADE` | Xóa user → xóa lượt thi (an toàn dữ liệu cá nhân) |
| `CLASS_STUDENTS` | `ON DELETE CASCADE` ở cả 2 phía | Xóa lớp hoặc học viên → dọn quan hệ |

### 6.3. CHECK constraint — ràng buộc giá trị

Đảm bảo dữ liệu chỉ nhận một tập giá trị hợp lệ:

- `USERS.ROLE IN ('ADMIN','TEACHER','STUDENT')`
- `USERS.STATUS IN ('ACTIVE','LOCKED')`
- `QUESTIONS.CORRECT_ANSWER IN ('A','B','C','D')`
- `QUESTIONS.DIFFICULTY IN ('EASY','MEDIUM','HARD')`
- `QUESTIONS.POINT > 0 AND POINT <= 100`
- `EXAMS.STATUS IN ('DRAFT','PUBLISHED','CLOSED')`
- `EXAMS.END_TIME > START_TIME`
- `EXAMS.DURATION_MINUTES > 0`
- `EXAM_ATTEMPTS.STATUS IN ('IN_PROGRESS','SUBMITTED','EXPIRED')`

### 6.4. UNIQUE constraint

- `USERS.EMAIL` — email không trùng.
- `SUBJECTS.CODE` — mã môn không trùng.
- `CLASSES.NAME` — tên lớp không trùng.
- `EXAM_ATTEMPTS(EXAM_ID, STUDENT_ID)` — mỗi học viên chỉ thi 1 lần / 1 đề.
- `EXAM_RESULTS(EXAM_ID, STUDENT_ID)` — mỗi học viên chỉ có 1 kết quả / 1 đề.

---

## 7. Chỉ mục (Indexes)

Schema có **9 indexes** được tạo thủ công nhằm tăng tốc các truy vấn thường xuyên:

| Index | Bảng | Cột | Mục đích |
|---|---|---|---|
| `IDX_QUESTIONS_SUBJECT` | QUESTIONS | SUBJECT_ID | Lọc câu hỏi theo môn |
| `IDX_QUESTIONS_DIFFICULTY` | QUESTIONS | (SUBJECT_ID, DIFFICULTY) | Random câu hỏi theo độ khó |
| `IDX_CLASS_STUDENTS_STUDENT` | CLASS_STUDENTS | STUDENT_ID | Tra cứu lớp của học viên |
| `IDX_EXAMS_SUBJECT_STATUS` | EXAMS | (SUBJECT_ID, STATUS) | Lọc đề theo môn & trạng thái |
| `IDX_EXAMS_STATUS_START` | EXAMS | (STATUS, START_TIME, END_TIME) | Tìm đề đang mở trong khung giờ |
| `IDX_EXAM_QUESTIONS_Q` | EXAM_QUESTIONS | QUESTION_ID | Tra cứu đề chứa 1 câu hỏi |
| `IDX_EXAM_CLASSES_CLASS` | EXAM_CLASSES | CLASS_ID | Tra cứu đề của 1 lớp |
| `IDX_ATTEMPTS_STUDENT` | EXAM_ATTEMPTS | STUDENT_ID | Lịch sử thi của học viên |
| `IDX_VIOLATIONS_ATTEMPT` | VIOLATIONS | (ATTEMPT_ID, OCCURRED_AT) | Nhật ký vi phạm theo lượt thi |
| `IDX_RESULTS_STUDENT` | EXAM_RESULTS | STUDENT_ID | Kết quả của học viên |

Ngoài ra Oracle **tự động tạo index** cho mọi `PRIMARY KEY` và `UNIQUE` constraint.

---

## 8. Kết nối từ Backend

Backend Node.js kết nối Oracle thông qua **driver `oracledb` 6.x** với **connection pool** để xử lý nhiều request đồng thời.

### 8.1. Cấu hình môi trường (`.env`)

```env
DB_USER=trac_nghiem
DB_PASSWORD=********
DB_CONNECT_STRING=localhost:1521/ORCLPDB1
```

### 8.2. Cấu trúc truy cập dữ liệu

```
HTTP Request
    ↓
Express Router
    ↓
Controller (validate, unwrap)
    ↓
Service (business logic)
    ↓
Repository (SQL thuần, dùng oracledb pool)
    ↓
Oracle 19c
```

Mọi câu SQL đều đặt trong tầng **repository** để tách biệt hoàn toàn với logic nghiệp vụ, giúp dễ kiểm thử và bảo trì.

### 8.3. Đặc điểm truy vấn

- Tất cả truy vấn dùng **bind parameters** (`:id`, `:email`...) để chống SQL Injection.
- Kết quả trả về theo **envelope thống nhất**: `{ success: true, data, message? }`.
- Với các danh sách có phân trang: `{ items, pagination: { page, limit, total } }`.

---

## 9. Cài đặt & khởi tạo

### 9.1. Yêu cầu

- Oracle Database 19c (hoặc Oracle Live SQL cho demo).
- User có quyền `CREATE TABLE`, `CREATE INDEX`, `CREATE SEQUENCE`.
- Nếu chạy Node.js từ Windows và cần kết nối trực tiếp, cài **Oracle Instant Client 19c** và trỏ `OCI_LIB_DIR` / `PATH` tới thư mục `instantclient`.

### 9.2. Khởi tạo schema

Kết nối bằng SQL*Plus / SQLcl / DBeaver rồi chạy các file trong `backend/database/`:

```sql
@database/schema.sql   -- tạo bảng + index
@database/seed.sql     -- (tuỳ chọn) chèn dữ liệu mẫu
```

Hoặc dùng tool seed tài khoản admin:

```bash
npm run seed:admin     # tạo admin@local với mật khẩu Admin@123
```

### 9.3. Xóa sạch schema (khi cần reset)

```sql
@database/drop.sql
```

---

## 10. Các tính năng nổi bật của CSDL trong dự án

### 10.1. Toàn vẹn dữ liệu ở tầng DB

- Toàn bộ ràng buộc nghiệp vụ quan trọng (`ROLE`, `STATUS`, `DIFFICULTY`, `END_TIME > START_TIME`, `POINT > 0`...) đều được đẩy xuống `CHECK constraint` ở database.
- Ngay cả khi backend có lỗi, dữ liệu "bẩn" vẫn không thể được insert vào DB.

### 10.2. Đảm bảo "1 lượt thi duy nhất"

`UNIQUE(EXAM_ID, STUDENT_ID)` trên `EXAM_ATTEMPTS` khiến một học viên không thể bị tạo 2 lượt thi cho cùng một đề — đây là một ràng buộc nghiệp vụ cốt lõi.

### 10.3. Xóa mềm tài khoản

Thay vì xóa cứng user, hệ thống dùng `STATUS = 'LOCKED'`. Điều này giữ nguyên các bản ghi lịch sử (`EXAMS.CREATED_BY`, `EXAM_ATTEMPTS.STUDENT_ID`, `EXAM_RESULTS.STUDENT_ID`) với `ON DELETE SET NULL` — vừa bảo toàn lịch sử, vừa không vi phạm khóa ngoại.

### 10.4. Hỗ trợ tiếng Việt đầy đủ

Tất cả cột văn bản dùng `VARCHAR2(n CHAR)` — số ký tự tính theo `CHAR` chứ không phải `BYTE`, giúp hiển thị đúng tiếng Việt có dấu.

### 10.5. Index cho truy vấn nóng

Các truy vấn "nóng" (danh sách đề đang mở, lịch sử thi của học viên, random câu hỏi theo độ khó...) đều có index phù hợp — quan trọng cho hệ thống có nhiều giáo viên và học viên truy cập đồng thời.

### 10.6. Sẵn sàng cho các mở rộng nâng cao

Schema được thiết kế đủ "chuẩn hóa" để có thể bổ sung:

- **Trigger** tự động cập nhật `UPDATED_AT`, ghi log thay đổi trạng thái đề thi.
- **Stored Procedure** cho nghiệp vụ chấm điểm phức tạp.
- **Function** tính điểm, xếp hạng học viên.
- **Materialized View** cho dashboard thống kê nhanh.

---

## 11. Cấu trúc file liên quan

```
backend/database/
├── schema.sql     # Định nghĩa 11 bảng + 9 indexes + các ràng buộc
├── seed.sql       # Dữ liệu mẫu (tùy chọn) + 1 index bổ sung
└── drop.sql       # Script xóa toàn bộ bảng (CASCADE)
```

- **`schema.sql`** là file định nghĩa schema chính — đọc file này để hiểu chi tiết cấu trúc bảng.
- **`seed.sql`** chèn 1 môn học mẫu và tạo thêm index `IDX_ATTEMPTS_EXAM` để tối ưu join.
- **`drop.sql`** dùng khi cần reset database về trạng thái trống.

---

## Tài liệu liên quan

- [`README.md`](../README.md) — Tổng quan dự án & luồng nghiệp vụ.
- [`backend/README.md`](../backend/README.md) — Hướng dẫn cài đặt & chạy backend.
- [`DATABASE_PRESENTATION.md`](../DATABASE_PRESENTATION.md) — Tài liệu chi tiết về schema, DDL/DML, truy vấn, Trigger, Procedure (dùng cho thuyết trình học thuật).
- File SQL: [`schema.sql`](../backend/database/schema.sql), [`seed.sql`](../backend/database/seed.sql), [`drop.sql`](../backend/database/drop.sql).